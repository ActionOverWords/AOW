/* AOW Index data — District of Columbia (DC).
   Federal (Senate/House) and state-level officials whose state code is 'DC',
   plus their promises. Part of the split civic dataset — see data/README.md.
   Pushes into DEFAULT_MEMBERS / DEFAULT_PROMISES, declared (empty) in data.js,
   which must load before this file. Sorted alphabetically by name. */
DEFAULT_MEMBERS.push(
  {id:'del-norton', name:'Eleanor Holmes Norton', initials:'EN', level:'federal', chamber:'House', party:'D', state:'DC', district:0, nonVoting:true, seat:'House · Delegate, District of Columbia', termStart:'2025'},
);
